<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mapp.noted">
    <uses-feature android:name="android.hardware.camera"
        android:required="true" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="mapp.noted.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="mapp.noted.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="mapp.noted.AddNotes"
            android:parentActivityName="mapp.noted.MainActivity">

            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="mapp.noted.MainActivity" />
        </activity>
        <activity android:name="mapp.noted.UpdateNote"
            android:parentActivityName="mapp.noted.MainActivity">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="mapp.noted.MainActivity" />
        </activity>
        <activity android:name="mapp.noted.ViewNote"
            android:parentActivityName="mapp.noted.MainActivity">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="mapp.noted.MainActivity" />
        </activity>
        <activity android:name="mapp.noted.MainPage"
            android:parentActivityName="mapp.noted.MainActivity">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="mapp.noted.MainActivity" />
        </activity>
        <activity android:name="mapp.noted.ViewPage"
            android:parentActivityName="mapp.noted.MainActivity">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="mapp.noted.MainActivity" />
        </activity>
        <activity android:name="mapp.noted.Search"
            android:parentActivityName="mapp.noted.MainActivity">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="mapp.noted.MainActivity" />
        </activity>
        <activity android:name="mapp.noted.RecentPage"
            android:parentActivityName="mapp.noted.MainActivity">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="mapp.noted.MainActivity" />
        </activity>
        <activity android:name="mapp.noted.About"
            android:parentActivityName="mapp.noted.MainActivity">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="mapp.noted.MainActivity" />
        </activity>
        <activity android:name="mapp.noted.MonthPage"
            android:parentActivityName="mapp.noted.MainActivity">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="mapp.noted.MainActivity" />
        </activity>
        <activity android:name="mapp.noted.Secret"
            android:parentActivityName="mapp.noted.MainActivity">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="mapp.noted.MainActivity" />
        </activity>
        <activity android:name="mapp.noted.ViewSecret"
            android:parentActivityName="mapp.noted.MainActivity">
            <!-- The meta-data element is needed for versions lower than 4.1 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="mapp.noted.MainActivity" />
        </activity>
    </application>

</manifest>